<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RhythmChat</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            width: 100vw;
            min-height: 100vh;
            background: #4D4E56;
            overflow-x: hidden;
        }

        .wrapper {
            width: 100%;
            height: 100vh;
            display: flex;
            gap: 10px;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
    </style>
</head>

<body>
    <div class="wrapper">
        <h2 style="text-align: center;">
            <%= locals.msg %>
        </h2>
        <% if (locals.msg!=='Password Successfully Reset.' ) { %>
            <p onclick='goBack()' style="color: white; cursor: pointer;">go back</p>
            <% } %>
    </div>
</body>

<script>
    function goBack() {
        window.history.back();
    }
</script>

</html>