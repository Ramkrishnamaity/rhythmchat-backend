<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RhythmChat</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            width: 100vw;
            min-height: 100vh;
            background: #4D4E56;
            overflow-x: hidden;
        }

        .wrapper {
            width: 100%;
            height: 100vh;
            display: flex;
            gap: 2rem;
            padding: 10px;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
    </style>
</head>

<body>
    <div class="wrapper">
        <% if (locals.message) { %>
            <div>
                <%= locals.message %>
            </div>
            <% } else { %>
                <h2 style="text-align: center; color: white;">Hlo <span
                        style="color: black; text-transform: lowercase;">
                        <%= locals.name %>
                    </span> reset your password from here.
                </h2>
                <div style="width: max-content; height: max-content; padding-top: 20px;">
                    <form action="/api/v1/user/change-password/<%= locals.id %>" method="POST">
                        <div
                            style="display: flex; flex-direction: column; gap: 10px; font-size: medium; font-weight: 500;">
                            <label for="password">Enter New Password:</label>
                            <input type="text" name="password" required
                                style="height: 30px; min-width: 200px; outline: none; padding-inline: 5px; letter-spacing: 2px; color: white; background-color: #282B34;" />
                        </div>
                        <div
                            style="display: flex; margin-top: 15px; margin-bottom: 25px; flex-direction: column; gap: 10px; font-size: medium; font-weight: 500;">
                            <label for="confirmPassword">Confirm New Password:</label>
                            <input type="text" name="confirmPassword" required
                                style="height: 30px; min-width: 200px; outline: none; padding-inline: 5px; letter-spacing: 2px; color: white; background-color: #282B34;" />
                        </div>
                        <div style="display: flex; align-items: center; justify-content: center; min-width: 200px;">
                            <button type="submit"
                                style="padding: 4px 8px; border-radius: 4px; border: 2px transparent;">
                                Submit
                            </button>
                        </div>
                    </form>
                </div>
                <% } %>


    </div>
</body>

</html>